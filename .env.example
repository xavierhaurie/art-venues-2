# ==================== SUPABASE CONFIGURATION ====================
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api

# Supabase Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anon/Public Key (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (NEVER expose to browser - server-side only)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ==================== DATABASE CONFIGURATION ====================
# Direct database connection (for migrations/admin tasks)
DATABASE_URL=postgresql://postgres:your-password@db.your-project-ref.supabase.co:5432/postgres

# ==================== AUTHENTICATION SECRETS ====================
# JWT Secret for signing tokens (generate with: openssl rand -base64 32)
JWT_SECRET=your-jwt-secret-here-minimum-32-characters

# Session encryption key (generate with: openssl rand -base64 32)
SESSION_SECRET=your-session-secret-here-minimum-32-characters

# Magic link token secret (generate with: openssl rand -base64 32)
MAGIC_LINK_SECRET=your-magic-link-secret-here

# ==================== OAUTH PROVIDER CREDENTIALS ====================
# Google OAuth (get from: https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Facebook OAuth (get from: https://developers.facebook.com/apps/)
FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret

# ==================== EMAIL SERVICE (for magic links) ====================
# Choose one email provider and configure accordingly

# Option 1: Resend (recommended - https://resend.com)
EMAIL_PROVIDER=resend
RESEND_API_KEY=re_your_resend_api_key

# Option 2: SendGrid (https://sendgrid.com)
# EMAIL_PROVIDER=sendgrid
# SENDGRID_API_KEY=SG.your_sendgrid_api_key

# Option 3: AWS SES
# EMAIL_PROVIDER=ses
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1

# Email sender configuration
EMAIL_FROM_ADDRESS=noreply@yourdomain.com
EMAIL_FROM_NAME=Art Venues

# ==================== APPLICATION CONFIGURATION ====================
# Application URL (used for OAuth callbacks and magic links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment
NODE_ENV=development

# ==================== RATE LIMITING ====================
# Rate limit configuration (requests per window)
RATE_LIMIT_MAGIC_LINK=3
RATE_LIMIT_WINDOW_MINUTES=15
RATE_LIMIT_TOTP=5
RATE_LIMIT_OAUTH=10

# ==================== SESSION CONFIGURATION ====================
# Session duration in seconds (default: 7 days)
SESSION_DURATION=604800

# Session rotation on 2FA (enables session rotation when TOTP is enabled)
SESSION_ROTATE_ON_2FA=true

# ==================== TOTP CONFIGURATION ====================
# TOTP issuer name (shown in authenticator apps)
TOTP_ISSUER=Art Venues

# Number of backup codes to generate
TOTP_BACKUP_CODES_COUNT=10

# ==================== SECURITY CONFIGURATION ====================
# CORS allowed origins (comma-separated for multiple)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Cookie security settings
COOKIE_SECURE=false
COOKIE_SAME_SITE=lax
COOKIE_HTTP_ONLY=true

# ==================== OPTIONAL: LOGGING & MONITORING ====================
# Log level (debug, info, warn, error)
LOG_LEVEL=info

# Sentry DSN (for error tracking - https://sentry.io)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ==================== OPTIONAL: STORAGE ====================
# Supabase Storage bucket for artist media
STORAGE_BUCKET_ARTIST_MEDIA=artist-media
STORAGE_BUCKET_VENUE_IMAGES=artwork

# Maximum file upload size (in bytes - default 10MB)
MAX_FILE_SIZE=10485760

# ==================== DEVELOPMENT ONLY ====================
# Skip email in development (log magic links to console instead)
DEV_SKIP_EMAIL=true

# Admin user email for seeding
DEV_ADMIN_EMAIL=admin@example.com

# ==================== NOTES ====================
# 1. Copy this file to .env.local for development
# 2. Never commit .env.local or .env with real values to git
# 3. Generate strong secrets with: openssl rand -base64 32
# 4. Update NEXT_PUBLIC_APP_URL for production deployment
# 5. Set COOKIE_SECURE=true in production
# 6. Set DEV_SKIP_EMAIL=false in production

